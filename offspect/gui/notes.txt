GUI notes

Previous and next buttons
	keyboard input
	dragging plot?

Other keyboard inputs

latency change as number input
	tracking

coil coordinates
	coil relative to map grid (brain would be extra cool)
	for each map location
	not stored in xdf files
	
	all points in grey
	current in red

plot window whole width of main window


fields from each each trace
	time since last pulse
	onset shift
	di/dt

hidden menus (and what to include)

Always shown (ini file):
	muscle
	readout

origin file indicator

keyboard mapping (video game controls) (ini file)

manual scaling of plot

reject trial button do/undo
	with corresponding indication (color)

examiner per session, not trace

parameter changes override examiner